# Example fleet file — copy and edit, then:
#   bash launch.sh ... --fleet-file ./my-fleet.txt
#
# Format:
#   shortname=provider/model[:thinking]   # kind=pi
#   shortname=kind:model                  # non-pi herdr kind (cursor/codex/...)
# shortname must match ^[a-z][a-z0-9_-]{0,31}$ (will be namespaced under --label).
#
# Discover models:
#   pi --list-models
#   pi --list-models deepseek
#   agent --list-models          # cursor-cli
#   herdr agent                  # supported kinds
#
# Shipped profiles (author-specific; only work if configured):
#   fleet.defaults  — usual seven (daily)
#   fleet.full      — full eight (heavy)

# Minimal 3-model example (replace with models you actually have):
# rev-a=codex:gpt-6-astra:high
# rev-b=dsh:deepseek-flash:max
# rev-c=cursor:claude-fable-5-1-thinking-high

# Author's usual seven (same as fleet.defaults):
gpt6astra=codex:gpt-6-astra:high
dsv4flash=dsh:deepseek-flash:max
glm53=siliconflow/zai-org/GLM-5.3:max
fable51=cursor:claude-fable-5-1-thinking-high
k3max=cursor:kimi-k3-max
g38flash=cursor:gemini-3.8-flash-high
musespark=cursor:muse-spark-1.3-max
