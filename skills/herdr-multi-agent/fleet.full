# Heavy / max-diversity fleet — author's full eleven.
# Use when quota allows or for high-stakes reviews:
#   bash launch.sh ... --fleet-file "$SKILL_DIR/fleet.full"
#
# Daily default is fleet.defaults (usual eight). Same line format.
# Add-back order from seven → ten when opencode-go quota improves:
#   mimopro → hy3  (+ dsv4flash for non-SF DeepSeek path)
# Daily Go seat is glm53 (replaced qwen38max). Keep glm52 as the free SF vote.
# Kimi K3 is cursor-cli only (k3max); opencode-go k3 is out of both fleets.
# qwen38max is out unless explicitly --agent'd.

gpt56sol=openai-codex/gpt-5.6-sol:xhigh
glm53=opencode-go/glm-5.3:max
mimopro=opencode-go/mimo-v2.5-pro:max
hy3=opencode-go/hy3:max
dsv4pro=siliconflow/deepseek-ai/DeepSeek-V4-Pro:high
dsv4flash=deepseek/deepseek-v4-flash:max
glm52=siliconflow/zai-org/GLM-5.2:max
k27code=siliconflow/moonshotai/Kimi-K2.7-Code:high
dots3=dots/dots3-note-prev:max
fable5=cursor:claude-fable-5-thinking-high
k3max=cursor:kimi-k3-max
